# Public (used in browser). Set to your CMS API base URL when backend is ready.
# NEXT_PUBLIC_CMS_API_URL=https://api.tanzaniawildmakersafari.com/api/v1


NEXT_PUBLIC_CMS_API_URL=http://localhost:3000/api/v1


# Optional: CMS login password (placeholder until backend provides real auth).
# Default in dev is "admin" if unset.
# NEXT_PUBLIC_CMS_PASSWORD=your-secret-password
